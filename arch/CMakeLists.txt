IF (${TARGET_PROCESSOR_AARCH64})
ADD_SUBDIRECTORY(aarch64)
ENDIF (${TARGET_PROCESSOR_AARCH64})

ADD_LIBRARY(ArchLib STATIC dummy.c)

IF (NOT ${ONLY_TESTS})
TARGET_LINK_LIBRARIES(ArchLib PRIVATE ArchLibProper)
ENDIF()