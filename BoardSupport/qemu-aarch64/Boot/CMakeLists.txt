LIST(APPEND BOOT_HEADERS BootDriver.hpp)
LIST(APPEND BOOT_SOURCES BootDriver.cpp)

LIST(APPEND BOOT_SOURCES Boot.cpp)

ADD_LIBRARY(Boot STATIC ${BOOT_HEADERS} ${BOOT_SOURCES})
TARGET_LINK_LIBRARIES(Boot ArchLib)

SET_TARGET_PROPERTIES(Boot PROPERTIES LINK_DEPENDS ${LINKER_SCRIPT})