LIST(APPEND COMMON_HEADERS kern_integers.hpp)
LIST(APPEND COMMON_HEADERS kern_macro.hpp)

LIST(APPEND COMMON_SOURCES CommonRuntime.cpp)

INCLUDE_DIRECTORIES(PhyProtocol)
ADD_SUBDIRECTORY(PhyProtocol)

ADD_LIBRARY(Common STATIC
	${COMMON_HEADERS}
	${COMMON_SOURCES})

TARGET_LINK_LIBRARIES(Common PRIVATE PhyProtocol)